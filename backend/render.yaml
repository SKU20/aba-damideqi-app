services:
  - type: web
    name: aba-damideqi-app
    env: node
    buildCommand: |
      # Install Node.js dependencies
      npm install
      
      # Install Python and dependencies
      apt-get update
      apt-get install -y python3 python3-pip
      pip3 install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
